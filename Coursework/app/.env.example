# Railway PostgreSQL connection string
DATABASE_URL=postgresql://username:password@hostname:5432/database

# Default household id for member inserts when no household is selected
DEFAULT_HOUSEHOLD_ID=1

# Port used by the app (Railway will provide PORT automatically)
PORT=3000
